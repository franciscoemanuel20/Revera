# =============================================================================
# Reverá — variáveis de ambiente (SOMENTE NOMES; nunca commitar valores)
# Sem projeto Supabase real ainda — ver README, "decisões pendentes".
# =============================================================================

# --- Supabase (projeto ainda não existe; criar exige autorização do Francisco) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- Pagamento ---
# 'mock' é o único provider implementado neste scaffold (src/lib/payments).
# 'infinitepay' existe só como valor válido da env — o adapter real lança
# erro de propósito até o gateway ser decidido e implementado.
PAYMENT_PROVIDER=mock
# # Obsoleta: a API da InfinitePay identifica a conta pelo handle, não por
# chave secreta. O código lê INFINITEPAY_HANDLE (mais abaixo). Mantida vazia
# só para não quebrar ambiente antigo que ainda a defina.
INFINITEPAY_API_KEY=            # preencher quando decidido

# --- Frete ---
# A presença do TOKEN é o que troca o mock pela SuperFrete de verdade — não
# há uma variável "SHIPPING_PROVIDER" separada. Consequência a ter em mente:
# no instante em que o token entrar na Vercel, o frete real passa a valer,
# sem deploy. Por isso ele é a ÚLTIMA coisa a configurar.
#
# O token é POR CONTA, e a conta é a mesma carteira de onde saem as etiquetas.
# Pode ser o mesmo do site irmão (mesma operação, mesmo endereço de origem);
# nesse caso as etiquetas das duas marcas aparecem juntas no painel deles e
# consomem o mesmo saldo.
SUPERFRETE_TOKEN=
# OBRIGATÓRIA quando SUPERFRETE_TOKEN existe. Aceita "1" (sandbox) ou "0"
# (produção) — e MAIS NADA. Qualquer outro valor faz a cotação falhar alto,
# de propósito (P0-4, 27/08/2026): antes a comparação era `=== "1"` e tudo
# que não fosse "1" caía em produção calado. Em 26/08 esta variável continha
# o token de produção colado por engano, e o ambiente de desenvolvimento
# passou a falar com a API de produção acreditando estar em sandbox.
# NÃO cole token aqui. Token é SUPERFRETE_TOKEN.
SUPERFRETE_SANDBOX=

# Identificação exigida pela SuperFrete em toda requisição (eles recusam sem
# ela). O padrão no código já identifica a Reverá — só preencha para mudar.
SUPERFRETE_USER_AGENT=

# --- De onde a peça sai ---
# Todos têm padrão no código (Rua Síria 71, sala 39 — São José dos Campos/SP,
# CEP 12216-530, conferido na base dos Correios em 19/08/2026). Endereço
# comercial não é segredo, e assim a cotação funciona sem configuração
# nenhuma. Preencha só para mudar de endereço — o que não exige deploy.
SUPERFRETE_CEP_ORIGEM=
SUPERFRETE_REMETENTE_NOME=
SUPERFRETE_REMETENTE_RUA=
SUPERFRETE_REMETENTE_NUMERO=
SUPERFRETE_REMETENTE_COMPLEMENTO=
SUPERFRETE_REMETENTE_BAIRRO=
SUPERFRETE_REMETENTE_CIDADE=
SUPERFRETE_REMETENTE_UF=

# --- A caixa ---
# Padrão: 300 g em 30 × 20 × 5 cm, por peça. Erra para cima de propósito —
# ver o comentário em src/lib/shipping/regras.ts. Medir a embalagem real e
# preencher aqui é a única coisa que falta para o frete parar de ser
# aproximado.
SUPERFRETE_CAIXA_PESO_GRAMAS=
SUPERFRETE_CAIXA_COMPRIMENTO_CM=
SUPERFRETE_CAIXA_LARGURA_CM=
SUPERFRETE_CAIXA_ALTURA_CM=

# --- Rastreamento ---
#
# ATENÇÃO À DIFERENÇA ENTRE OS DOIS GRUPOS ABAIXO.
#
# ID de pixel NÃO é segredo: aparece no fonte de qualquer site que o use.
# Por isso vem com NEXT_PUBLIC_, que o Next expõe ao navegador de propósito.
#
# TOKEN de API É segredo, e vem SEM o prefixo. Um token de CAPI que vaze para
# o bundle deixa qualquer pessoa forjar conversões na conta de anúncios.
# scripts/verify-no-secrets-in-bundle.mjs confere isso a cada build.
#
# Nada aqui é obrigatório. Sem os IDs, nenhum script de terceiro é carregado
# e o site funciona igual — os envios ficam registrados em conversion_logs
# dizendo qual variável está vazia.

# Públicas (vão para o navegador):
NEXT_PUBLIC_META_PIXEL_ID=
# A tag do Google. GT-… carrega GA4 e Google Ads juntos; G-… mede só GA4.
NEXT_PUBLIC_GOOGLE_TAG_ID=
# Sempre G-…, mesmo quando a de cima é GT-. Usada no envio de servidor.
NEXT_PUBLIC_GA4_MEASUREMENT_ID=

# Segredos (só servidor):
# Meta → Gerenciador de Eventos → Configurações → Conversions API → gerar token
META_CAPI_TOKEN=
# GA4 → Admin → Fluxos de dados → o fluxo → Measurement Protocol API secrets
GA4_API_SECRET=

# --- WhatsApp pós-compra ---
# 12981409901 — usar SOMENTE em componentes pós-compra, nunca em página
# pré-compra ou metadata (ver docs internos do projeto sobre contato único).
# WHATSAPP_POST_PURCHASE_NUMBER=

# Segredo do caminho do webhook de pagamento.
# Gere com: openssl rand -base64 32
# Sem ela o pagamento NÃO abre (proposital: no projeto irmão a ausência
# saiu calada e nenhuma venda se confirmou por dias).
PAYMENT_WEBHOOK_SECRET=

# InfiniteTag da conta InfinitePay, SEM o "$". Só necessária quando
# PAYMENT_PROVIDER=infinitepay.
#
# ELA DECIDE PARA QUAL CONTA O DINHEIRO VAI. A InfinitePay identifica o
# recebedor por este campo enviado no corpo — não há chave secreta nesta API.
# Handle errado não quebra a venda: cria o link normalmente e credita outra
# conta, o que só aparece na conciliação ou na NF-e.
#
# A da Reverá é `reveraproteses` — REVERA PROTESE CAPILAR E COMERCIO LTDA,
# CNPJ 62.118.672/0001-53. Conferido em 31/08/2026 na própria tela de
# pagamento da InfinitePay, que mostra a razão social do recebedor.
#
# Como conferir sem o painel: faça um checkout de teste em produção e olhe o
# nome no topo da tela da InfinitePay. Não precisa pagar.
INFINITEPAY_HANDLE=

# URL pública do site, usada para montar webhook e redirect do gateway.
NEXT_PUBLIC_SITE_URL=


# ---------------------------------------------------------------------------
# Rastreamento fora de produção (P0-3, 27/08/2026)
# ---------------------------------------------------------------------------
# Por padrão, NADA é enviado ao Pixel/CAPI fora de produção — nem o pixel base
# carrega. Isso impede que navegação de desenvolvimento e compras simuladas
# entrem na conta de anúncios real.
#
# Para exercitar a integração de verdade sem sujar a conta, defina AS DUAS:
#   TRACKING_ALLOW_DEV_SEND=1
#   META_TEST_EVENT_CODE=<código do Gerenciador de Eventos da Meta>
# Só com as duas o envio acontece, e vai marcado como TESTE (a Meta separa
# esses eventos e não os usa para otimizar campanha).
#
# NUNCA defina nenhuma das duas em produção — a trava de deploy recusa.
TRACKING_ALLOW_DEV_SEND=
META_TEST_EVENT_CODE=

# ---------------------------------------------------------------------------
# Stripe — gateway INTERNACIONAL (USD/EUR/GBP/AUD/CAD). BRL fica na InfinitePay.
# As duas andam juntas; teste (sk_test_) só fora de produção, live só em
# produção — verify-deploy-seguro.mjs recusa o contrário.
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# Aponta o adapter para o dublê local (scripts/stripe-fake.mjs) — SÓ em
# desenvolvimento/staging; recusada onde há comprador real.
# STRIPE_API_BASE=
# Países abertos no checkout além do Brasil (ISO-2, separados por vírgula).
# Um país só abre de fato com Stripe + preço do mercado + cotação de frete
# vigente — a variável sozinha não vende nada.
# CHECKOUT_PAISES=BR
